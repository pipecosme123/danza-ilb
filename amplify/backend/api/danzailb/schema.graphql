type Users @model 
@auth(rules: [{ allow: public }]) 
@key(name: "byNumId", fields: ["numId"], queryField: "getByNumId"){
  id: ID!
  tipoId: String!
  numId: String!
  nombres: String!
  apellidos: String!
  fechaNacimiento: String!
  genero: String!
  direccion: String!
  telefono: String!
}
